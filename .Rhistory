make_unique = TRUE)
Clustered_DotPlot(seurat_object = seur, features = top_markers, colors_use_idents = cols1, k=11, plot_km_elbow = F)
Idents(seu) <- seu$celltype
all_markers <- FindAllMarkers(object = seu) %>%
Add_Pct_Diff() %>% dplyr::filter(pct_diff > 0.6)
top_markers <- Extract_Top_Markers(marker_dataframe = all_markers, num_genes = 5, named_vector = FALSE,
make_unique = TRUE)
Clustered_DotPlot(seurat_object = seur, features = top_markers, colors_use_idents = cols1, k=11, plot_km_elbow = F)
# Calculate and plot gene dispersion in query dataset
seu <- calculate_gene_dispersion(seu)
seu <- select_genes(seu, top_n = 10000, logmean_ul = -1, logmean_ll = -8)
plot_gene_dispersion(seu)
vgq <- get_selected_genes(seu)
# Repeat the process for the reference dataset
seur <- calculate_gene_dispersion(seur)
seur <- select_genes(seur, top_n = 10000, logmean_ul = -1, logmean_ll = -8)
plot_gene_dispersion(seur)
vgr <- get_selected_genes(seur)
# Find common genes
vg <- intersect(vgq, vgr)
#iterative LSI
comps <- 25 #Number of PCs for clustering
seur<-iterative_LSI(seur, num_dim = comps, num_features = length(vg), resolution = c(1e-3), verbose = T, starting_features = vg, run_umap = T)
roxygen2::roxygenise()
seur<-iterative_LSI(seur, num_dim = comps, num_features = length(vg), resolution = c(1e-3), verbose = T, starting_features = vg, run_umap = T)
seur<-iterative_LSI2(seur, num_dim = comps, num_features = length(vg), resolution = c(1e-3), verbose = T, starting_features = vg, run_umap = T)
seur<-iterative_LSI(seur, num_dim = comps, num_features = length(vg), resolution = c(1e-3), verbose = T, starting_features = vg, run_umap = T)
seur@reductions$lsi@misc
seur@reductions$lsi@misc$svd
seur<-iterative_LSI(seur, num_dim = comps, num_features = length(vg), resolution = c(1e-3, 1e-3), verbose = T, starting_features = vg, run_umap = T)
seur@reductions$lsi@misc$svd
p1 <- DimPlot(seur, group.by = "celltype", cols = cols1)+theme_void()+ggtitle("Reference - aka Projector Dataset after LSI/UMAP")
p1
res <- project_data(projector = seur, projectee = seu, reduced_dim = "lsi", embedding = "umap")
p2 <- plot_projection(res, seur, seu, projectee_col = "celltype")+scale_color_manual(values = cols2)+theme_void()+ guides(color = guide_legend(override.aes = list(size = 3)))
p2
cowplot::plot_grid(p1, p2)
roxygen2::roxygenise()
roxygen2::roxygenise()
pkgdown::deploy_to_branch()
pkgdown::deploy_to_branch()
# quick testing -----------------------------------------------------------
pkgdown::clean_site(pkg = ".")
pkgdown::init_site(pkg = ".")
pkgdown::deploy_to_branch()
pkgdown::deploy_to_branch()
pkgdown::preview_page('articles/LeukemiaFlowPlots.html')
rm(list=ls())
rm(list=ls())
suppressPackageStartupMessages({
library(monocle3)
library(dplyr)
library(Matrix)
library(ggplot2)
library(pals)
library(Seurat)
library(flscuts)
})
DATA_DIR <- "/Users/sfurla/Library/CloudStorage/OneDrive-SharedLibraries-FredHutchCancerCenter/Furlan_Lab - General/experiments/leuklong/integration"
#colors
ccol<-c("#FF0000", "#1C2170", "#009641", "#961E96", "#F47421", "#FFF000")
c2<-("58C6B8 B687D7 B2673D 393735 415988 E98597 F8D600 AD8752 C35C54 B29BDF CE281D 612B58 B0E9E6 CED5EB 2A5D67 752121 C35158 A8DF67 9B7250 EFB8E2 338246 7E378D F4950C BEBB81 294E55 C79E6C")
ctcol<-paste0("#", unlist(strsplit(c2, "\\ ")))
#devtools::install_github("furlan-lab/flscuts")
seu<-readRDS("/Users/sfurla/Library/CloudStorage/OneDrive-SharedLibraries-FredHutchCancerCenter/Furlan_Lab - General/datasets/Healthy_BM_greenleaf/230329_rnaAugmented_seurat.RDS")
#atac <- readRDS("/fh/working/furlan_s/sfurlan/pbjay/integration/cds/BM_atac.RDS")
# atac <- readRDS("~/Desktop/integration/cds/BM_atac.RDS")
atac <- readRDS(file.path(DATA_DIR, "cds", "BM_atac.RDS"))
pData(atac)$celltype <- pData(atac)$BioClassification
pData(atac)$celltype <- factor(pData(atac)$celltype)
new_levels <- c(
"HSC" = "01_HSC",
"Early.Eryth" = "02_Early.Eryth",
"Late.Eryth" = "03_Late.Eryth",
"Early.Baso" = "04_Early.Baso",
"CMP.LMPP" = "05_CMP.LMPP",
"CLP.1" = "06_CLP",
"CLP.2" = "06_CLP",  # If multiple match, decide how to handle
"GMP" = "07_GMP",
"GMP.Neut" = "08_GMP.Neut",
"pDC" = "09_pDC",
"cDC" = "10_cDC",
"CD14.Mono.1" = "11_CD14.Mono",
"CD14.Mono.2" = "11_CD14.Mono",  # Merging to a single level
"CD16.Mono" = "12_CD16.Mono",
"Pre.B" = "13_Pre.B",
"B" = "14_B",
"Plasma" = "15_Plasma",
"CD8.N" = "16_CD8.N",
"CD4.N1" = "17_CD4.N",
"CD4.N2" = "17_CD4.N",  # Merging subtypes
"CD4.M" = "18_CD4.M",
"CD8.EM" = "19_CD8.EM",
"CD8.CM" = "20_CD8.CM",
"NK" = "21_NK",
"Unk" = "22_Unk" # Keeping unknown as it is
)
levels(pData(atac)$celltype) <- new_levels[levels(pData(atac)$celltype)]
ctcol <- ctcol[-c(7,13,20)]
names(ctcol)<-levels(factor(pData(atac)$celltype))[order(levels(factor(pData(atac)$celltype)))]
#atacB <- atac
#atac <- atac[, sample(1:(dim(atac)[2]), 8000)]
pData(atac)$celltype<- factor(as.character(pData(atac)$celltype))
#atac <- atac[,!pData(atac)$celltype %in% "22_Unk"]
plot_cells(atac, color_cells_by = "celltype", cell_size = 0.6, label_cell_groups = F)+scale_color_manual(values=ctcol)
regions <- rownames(atac)
split_regions <- strsplit(regions, "_")
chromosomes <- sapply(split_regions, `[`, 1)
starts <- as.integer(sapply(split_regions, `[`, 2))
ends <- as.integer(sapply(split_regions, `[`, 3))
# Create GRanges object
gr <- GRanges(seqnames = chromosomes,
ranges = IRanges(start = starts, end = ends))
rowRanges(atac) <- gr
#load in
#DATA_DIR<-file.path("/fh/working/furlan_s/sfurlan/pbjay/integration/bulkfiles")
DATA_DIR<-file.path("~/Desktop/integration/bulkfiles")
dat<-data.table::fread(file.path(DATA_DIR,"GSE74912_ATACseq_All_Counts.txt"))
dat<-data.table::fread(file.path(DATA_DIR,"GSE74912_ATACseq_All_Counts.txt"))
DATA_DIR <- "/Users/sfurla/Library/CloudStorage/OneDrive-SharedLibraries-FredHutchCancerCenter/Furlan_Lab - General/experiments/leuklong/integration"
dat<-data.table::fread(file.path(DATA_DIR, "bulkfiles", "GSE74912_ATACseq_All_Counts.txt"))
meta<-data.table::fread(file.path(DATA_DIR, "bulkfiles", "GSE74912_ATACseq_meta.csv"), stringsAsFactors = F)
#munge a bit
gr<-GenomicRanges::GRanges(seqnames = dat$Chr, ranges = IRanges(start=dat$Start, end = dat$End))
rname<-paste(dat$Chr, dat$Start, end = dat$End, sep="_")
dat$Chr<-NULL
dat$Start<-NULL
dat$End<-NULL
rownames(dat)<-rname
se<-SummarizedExperiment(assays = list(counts=dat), rowRanges = gr)
se<-se[,colnames(se) %in% meta$Filedes]
colData(se)<-DataFrame(meta[match(colnames(se), meta$Filedes)], row.names = colnames(se))
se$ct<-se$source_name
se$ct2<-factor(se$ct)
blevels<-levels(factor(colData(atac)$BioClassification))
levels(se$ct2)<-c(blevels[c(16)], "01b_Blast", "01c_CD34_Bone_Marrow", "01d_CD34_Cord_Blood", blevels[c(22, 24, 15)], "05b_CMP", blevels[c( 3, 7, 1, 5)], "01e_LSC", blevels[c(2, 12)], "01f_MPP", blevels[c(25)], "01g_pHSC")
se$ct2<-as.character(se$ct2)
splitonce<-function(x, char="_") regmatches(x, regexpr(char, x), invert = TRUE)
rowRanges(se)
rowRanges(atac)
n_top <- c(50000, 50000) #Choose a higher number of variable peaks
PCs <- 50
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
debug(iterative_LSI)
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
roxygen2::roxygenise()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
roxygen2::roxygenise()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
debug(iterative_LSI)
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
roxygen2::roxygenise()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
debug(iterative_LSI)
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
roxygen2::roxygenise()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
rm(seu)
gc()
debug(tf_idf_transform)
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
rm(input)
gc()
gc()
object.size(mat) / 1024^2
object.size(mat)
object.size(mat)
class(mat)
object.size(mat@x) / 1024^2  # Check size in MB
object.size(mat@x)
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
roxygen2::roxygenise()
rm(list=ls())
gc()
suppressPackageStartupMessages({
library(monocle3)
library(dplyr)
library(Matrix)
library(ggplot2)
library(pals)
library(Seurat)
library(flscuts)
})
DATA_DIR <- "/Users/sfurla/Library/CloudStorage/OneDrive-SharedLibraries-FredHutchCancerCenter/Furlan_Lab - General/experiments/leuklong/integration"
#colors
ccol<-c("#FF0000", "#1C2170", "#009641", "#961E96", "#F47421", "#FFF000")
c2<-("58C6B8 B687D7 B2673D 393735 415988 E98597 F8D600 AD8752 C35C54 B29BDF CE281D 612B58 B0E9E6 CED5EB 2A5D67 752121 C35158 A8DF67 9B7250 EFB8E2 338246 7E378D F4950C BEBB81 294E55 C79E6C")
ctcol<-paste0("#", unlist(strsplit(c2, "\\ ")))
#atac <- readRDS("/fh/working/furlan_s/sfurlan/pbjay/integration/cds/BM_atac.RDS")
# atac <- readRDS("~/Desktop/integration/cds/BM_atac.RDS")
atac <- readRDS(file.path(DATA_DIR, "cds", "BM_atac.RDS"))
pData(atac)$celltype <- pData(atac)$BioClassification
pData(atac)$celltype <- factor(pData(atac)$celltype)
new_levels <- c(
"HSC" = "01_HSC",
"Early.Eryth" = "02_Early.Eryth",
"Late.Eryth" = "03_Late.Eryth",
"Early.Baso" = "04_Early.Baso",
"CMP.LMPP" = "05_CMP.LMPP",
"CLP.1" = "06_CLP",
"CLP.2" = "06_CLP",  # If multiple match, decide how to handle
"GMP" = "07_GMP",
"GMP.Neut" = "08_GMP.Neut",
"pDC" = "09_pDC",
"cDC" = "10_cDC",
"CD14.Mono.1" = "11_CD14.Mono",
"CD14.Mono.2" = "11_CD14.Mono",  # Merging to a single level
"CD16.Mono" = "12_CD16.Mono",
"Pre.B" = "13_Pre.B",
"B" = "14_B",
"Plasma" = "15_Plasma",
"CD8.N" = "16_CD8.N",
"CD4.N1" = "17_CD4.N",
"CD4.N2" = "17_CD4.N",  # Merging subtypes
"CD4.M" = "18_CD4.M",
"CD8.EM" = "19_CD8.EM",
"CD8.CM" = "20_CD8.CM",
"NK" = "21_NK",
"Unk" = "22_Unk" # Keeping unknown as it is
)
levels(pData(atac)$celltype) <- new_levels[levels(pData(atac)$celltype)]
ctcol <- ctcol[-c(7,13,20)]
names(ctcol)<-levels(factor(pData(atac)$celltype))[order(levels(factor(pData(atac)$celltype)))]
#atacB <- atac
#atac <- atac[, sample(1:(dim(atac)[2]), 8000)]
pData(atac)$celltype<- factor(as.character(pData(atac)$celltype))
#atac <- atac[,!pData(atac)$celltype %in% "22_Unk"]
plot_cells(atac, color_cells_by = "celltype", cell_size = 0.6, label_cell_groups = F)+scale_color_manual(values=ctcol)
regions <- rownames(atac)
split_regions <- strsplit(regions, "_")
chromosomes <- sapply(split_regions, `[`, 1)
starts <- as.integer(sapply(split_regions, `[`, 2))
ends <- as.integer(sapply(split_regions, `[`, 3))
# Create GRanges object
gr <- GRanges(seqnames = chromosomes,
ranges = IRanges(start = starts, end = ends))
rowRanges(atac) <- gr
debug(tf_idf_transform)
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
n_top <- c(50000, 50000) #Choose a higher number of variable peaks
PCs <- 50
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
object.size(mat)
roxygen2::roxygenise()
debug(tf_idf_transform)
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
rm(list=ls())
gc()
rm(list=ls())
gc()
suppressPackageStartupMessages({
library(monocle3)
library(dplyr)
library(Matrix)
library(ggplot2)
library(pals)
library(Seurat)
library(flscuts)
})
DATA_DIR <- "/Users/sfurla/Library/CloudStorage/OneDrive-SharedLibraries-FredHutchCancerCenter/Furlan_Lab - General/experiments/leuklong/integration"
#colors
ccol<-c("#FF0000", "#1C2170", "#009641", "#961E96", "#F47421", "#FFF000")
c2<-("58C6B8 B687D7 B2673D 393735 415988 E98597 F8D600 AD8752 C35C54 B29BDF CE281D 612B58 B0E9E6 CED5EB 2A5D67 752121 C35158 A8DF67 9B7250 EFB8E2 338246 7E378D F4950C BEBB81 294E55 C79E6C")
ctcol<-paste0("#", unlist(strsplit(c2, "\\ ")))
#devtools::install_github("furlan-lab/flscuts")
# seu<-readRDS("/Users/sfurla/Library/CloudStorage/OneDrive-SharedLibraries-FredHutchCancerCenter/Furlan_Lab - General/datasets/Healthy_BM_greenleaf/230329_rnaAugmented_seurat.RDS")
# DimPlot(seu, group.by = "SFClassification", cols = seu@misc$colors)
# levels(factor(seu$SFClassification))
```
split_regions <- strsplit(regions, "_")
chromosomes <- sapply(split_regions, `[`, 1)
starts <- as.integer(sapply(split_regions, `[`, 2))
ends <- as.integer(sapply(split_regions, `[`, 3))
# Create GRanges object
gr <- GRanges(seqnames = chromosomes,
ranges = IRanges(start = starts, end = ends))
rowRanges(atac) <- gr
#atac <- readRDS("/fh/working/furlan_s/sfurlan/pbjay/integration/cds/BM_atac.RDS")
# atac <- readRDS("~/Desktop/integration/cds/BM_atac.RDS")
atac <- readRDS(file.path(DATA_DIR, "cds", "BM_atac.RDS"))
pData(atac)$celltype <- pData(atac)$BioClassification
pData(atac)$celltype <- factor(pData(atac)$celltype)
new_levels <- c(
"HSC" = "01_HSC",
"Early.Eryth" = "02_Early.Eryth",
"Late.Eryth" = "03_Late.Eryth",
"Early.Baso" = "04_Early.Baso",
"CMP.LMPP" = "05_CMP.LMPP",
"CLP.1" = "06_CLP",
"CLP.2" = "06_CLP",  # If multiple match, decide how to handle
"GMP" = "07_GMP",
"GMP.Neut" = "08_GMP.Neut",
"pDC" = "09_pDC",
"cDC" = "10_cDC",
"CD14.Mono.1" = "11_CD14.Mono",
"CD14.Mono.2" = "11_CD14.Mono",  # Merging to a single level
"CD16.Mono" = "12_CD16.Mono",
"Pre.B" = "13_Pre.B",
"B" = "14_B",
"Plasma" = "15_Plasma",
"CD8.N" = "16_CD8.N",
"CD4.N1" = "17_CD4.N",
"CD4.N2" = "17_CD4.N",  # Merging subtypes
"CD4.M" = "18_CD4.M",
"CD8.EM" = "19_CD8.EM",
"CD8.CM" = "20_CD8.CM",
"NK" = "21_NK",
"Unk" = "22_Unk" # Keeping unknown as it is
)
levels(pData(atac)$celltype) <- new_levels[levels(pData(atac)$celltype)]
ctcol <- ctcol[-c(7,13,20)]
names(ctcol)<-levels(factor(pData(atac)$celltype))[order(levels(factor(pData(atac)$celltype)))]
#atacB <- atac
#atac <- atac[, sample(1:(dim(atac)[2]), 8000)]
pData(atac)$celltype<- factor(as.character(pData(atac)$celltype))
#atac <- atac[,!pData(atac)$celltype %in% "22_Unk"]
plot_cells(atac, color_cells_by = "celltype", cell_size = 0.6, label_cell_groups = F)+scale_color_manual(values=ctcol)
regions <- rownames(atac)
split_regions <- strsplit(regions, "_")
chromosomes <- sapply(split_regions, `[`, 1)
starts <- as.integer(sapply(split_regions, `[`, 2))
ends <- as.integer(sapply(split_regions, `[`, 3))
# Create GRanges object
gr <- GRanges(seqnames = chromosomes,
ranges = IRanges(start = starts, end = ends))
rowRanges(atac) <- gr
# reduction <- reducedDims(atac)[["LSI"]]
#
# colnames(reduction)<- paste0("LSI", colnames(reduction)
# seu[["LSI"]] <- CreateDimReducObject(reduction, key = "LSI")
#
# seu <- RunUMAP(seu, dims = 1:25, reduction = "LSI")
# DimPlot(seu, group.by = "BioClassification")+scale_color_manual(values=ctcol)
```
n_top <- c(50000, 50000) #Choose a higher number of variable peaks
PCs <- 50
debug(tf_idf_transform)
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
roxygen2::roxygenise()
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
debug(tf_idf_transform)
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
roxygen2::roxygenise()
debug(tf_idf_transform)
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
dim(mat)
mat
dim(mat)
mat
mat@p
diff(mat@p)
rep(idf, diff(mat@p))
idf   <- as(log(1 + ncol(mat) / row_sums), "sparseVector")
mat <- as(Matrix::Diagonal(x = as.vector(idf)), "sparseMatrix") %*%
mat
gc()
undebug(tf_idf_transform)
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
roxygen2::roxygenise()
gc()
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
roxygen2::roxygenise()
gc()
atac <- iterative_LSI(atac, num_dim = PCs, num_features = n_top, resolution = c(1e-3, 1.2e-3), verbose = T, binarize = T, run_umap = F)
gc()
atac <- reduce_dimension(atac, reduction_method = "UMAP", preprocess_method = "LSI", verbose = T)
plot_cells(atac, color_cells_by = "BioClassification", cell_size = 0.6, label_cell_groups = F)+scale_color_manual(values=ctcol)
plot_cells(atac, color_cells_by = "celltype", cell_size = 0.6, label_cell_groups = F)+scale_color_manual(values=ctcol)
gc()
dat<-data.table::fread(file.path(DATA_DIR, "bulkfiles", "GSE74912_ATACseq_All_Counts.txt"))
meta<-data.table::fread(file.path(DATA_DIR, "bulkfiles", "GSE74912_ATACseq_meta.csv"), stringsAsFactors = F)
#munge a bit
gr<-GenomicRanges::GRanges(seqnames = dat$Chr, ranges = IRanges(start=dat$Start, end = dat$End))
rname<-paste(dat$Chr, dat$Start, end = dat$End, sep="_")
dat$Chr<-NULL
dat$Start<-NULL
dat$End<-NULL
rownames(dat)<-rname
se<-SummarizedExperiment(assays = list(counts=dat), rowRanges = gr)
se<-se[,colnames(se) %in% meta$Filedes]
colData(se)<-DataFrame(meta[match(colnames(se), meta$Filedes)], row.names = colnames(se))
se$ct<-se$source_name
se$ct2<-factor(se$ct)
blevels<-levels(factor(colData(atac)$BioClassification))
levels(se$ct2)<-c(blevels[c(16)], "01b_Blast", "01c_CD34_Bone_Marrow", "01d_CD34_Cord_Blood", blevels[c(22, 24, 15)], "05b_CMP", blevels[c( 3, 7, 1, 5)], "01e_LSC", blevels[c(2, 12)], "01f_MPP", blevels[c(25)], "01g_pHSC")
se$ct2<-as.character(se$ct2)
splitonce<-function(x, char="_") regmatches(x, regexpr(char, x), invert = TRUE)
rowRanges(se)
rowRanges(atac)
n_top <- c(50000, 50000) #Choose a higher number of variable peaks
PCs <- 50
projection <- project_data(projector = atac, projectee = se, reduced_dim = "LSI", embedding = "UMAP", features = "range-based")
p<-as.data.frame(do.call(rbind, projection[2:1]))
p$ct<-colData(se)$ct2[match(p$Type, colnames(se))]
p$ct[is.na(p$ct)]<-"0_scATAC"
p$ct<-factor(p$ct)
levels(p$ct)<-splitonce(levels(p$ct)) %>% sapply("[[",2)
#add extra colors
ctcol<-c(ctcol, "01e_LSC"="lightsalmon", "01f_MPP"="turquoise",  "01g_pHSC"="cadetblue", "05b_CMP"="purple", "01c_CD34_Bone_Marrow"="darkcyan", "01d_CD34_Cord_Blood"="darkslategray4",  "01b_Blast"="firebrick1")
cols<-c("0_scATAC"="lightgray", ctcol)
ggplot(subset(p, !p$ct %in% levels(p$ct)[c(3:6,8)]), aes(x=UMAP1, y=UMAP2, color=ct))+geom_point(size=0.2, alpha=0.5)+ guides(colour = guide_legend(override.aes = list(size=5)))+monocle3:::monocle_theme_opts()+scale_color_manual(values=cols )
plot_cells(atac, color_cells_by = "BioClassification")
#' @export
#' @keywords internal
#'
#' @examples
#' \dontrun{
#' library(Seurat)
#' # Assuming 'seurat_obj' is a Seurat object with PCA and UMAP computed
#' seurat_obj <- find_partitions(seurat_obj, method = "leiden", k = 20, reduction = "umap", dims = 1:10)
#' # The partitions can be accessed via seurat_obj$partitions
#' }
find_partitions <- function(obj, method = "louvain", k = 20, reduction = "umap", dims = NULL, weight = FALSE,
num_iter = 1, resolution_parameter = NULL, random_seed = 2020,
verbose = TRUE, partition_q_value = 0.05) {
# Ensure necessary packages are available
if (!requireNamespace("Seurat", quietly = TRUE)) {
stop("Seurat package is required but not installed.")
}
if (!requireNamespace("igraph", quietly = TRUE)) {
stop("igraph package is required but not installed.")
}
# Check that obj is a Seurat object
if (!inherits(obj, "Seurat")) {
stop("Input object must be a Seurat object.")
}
# Check that reduction exists
if (!reduction %in% Seurat::Reductions(obj)) {
stop(paste0("Reduction '", reduction, "' not found in the Seurat object."))
}
# Determine dimensions to use
if (is.null(dims)) {
dims <- 1:ncol(Seurat::Embeddings(obj, reduction))
}
# Compute the nearest neighbor graph
obj <- Seurat::FindNeighbors(obj, reduction = reduction, dims = dims, k.param = k, verbose = verbose)
# Perform clustering
if (method == "leiden") {
obj <- Seurat::FindClusters(
obj,
algorithm = 4,
resolution = ifelse(is.null(resolution_parameter), 0.8, resolution_parameter),
random.seed = random_seed,
verbose = verbose
)
} else if (method == "louvain") {
obj <- Seurat::FindClusters(
obj,
algorithm = 1,
resolution = ifelse(is.null(resolution_parameter), 0.8, resolution_parameter),
random.seed = random_seed,
verbose = verbose
)
} else {
stop("Method must be either 'leiden' or 'louvain'.")
}
# Get the shared nearest neighbor (SNN) graph
graph_name <- paste0(Seurat::DefaultAssay(obj), "_snn")
snn_graph <- obj@graphs[[graph_name]]
# Convert the SNN graph to an igraph object
g <- graph_from_adjacency_matrix(snn_graph, mode = "undirected", weighted = TRUE)
snn_igraph <- igraph::as.igraph(g)
# Get the clusters
clusters <- as.character(Seurat::Idents(obj))
# Add cluster membership as a vertex attribute
snn_igraph <- igraph::set_vertex_attr(snn_igraph, "cluster", value = clusters)
# Build cluster graph
# For each edge, get the clusters of the two nodes
edges <- igraph::as_data_frame(snn_igraph, what = "edges")
vertices <- igraph::as_data_frame(snn_igraph, what = "vertices")
edges$cluster_from <- vertices$cluster[match(edges$from, vertices$name)]
edges$cluster_to <- vertices$cluster[match(edges$to, vertices$name)]
# Keep only edges between different clusters
cluster_edges <- edges[edges$cluster_from != edges$cluster_to, ]
# Create an edge list between clusters
cluster_edge_list <- unique(cluster_edges[, c("cluster_from", "cluster_to")])
# Build cluster graph
unique_clusters <- unique(c(cluster_edge_list$cluster_from, cluster_edge_list$cluster_to))
cluster_vertices <- data.frame(name = unique_clusters, stringsAsFactors = FALSE)
cluster_graph <- igraph::graph_from_data_frame(cluster_edge_list, directed = FALSE, vertices = cluster_vertices)
# Compute connected components
components <- igraph::components(cluster_graph)
cluster_partitions <- components$membership
names(cluster_partitions) <- igraph::V(cluster_graph)$name
# Map cells to partitions
cell_partitions <- cluster_partitions[clusters]
partitions <- as.factor(cell_partitions)
# Add partitions to metadata
obj$partitions <- partitions
message(paste0("Found ", length(unique(partitions)), " partitions!"))
return(obj)
}
